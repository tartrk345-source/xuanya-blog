# 变更日志

> 记录每次重大修改，按时间倒序排列。

---

## 2026-06-12

### 项目初始化
- 在 `D:\ai-blog\` 创建项目目录结构
- 创建 `README.md` — 项目总览
- 创建 `AGENTS.md` — AI 协作规则
- 创建 `docs/PRD.md` — 产品需求文档模板
- 创建 `docs/开发流程.md` — Vibe Coding 开发流程说明
- 创建 `docs/变更日志.md` — 本文件

### 第 1 步完成：明确产品目标
- 确定项目名：「博謇的个人博客」
- 确定范围：极简版 — 文章列表 + 详情 + Markdown 写作 + 编辑 + 删除 + localStorage
- 确定风格：杂志/阅读风
- 确定技术栈：Vite + React 18 + TypeScript + Tailwind CSS + react-markdown + react-router-dom
- 更新 `docs/PRD.md` 填写完整需求

### 第 1 步多角度评估 & 修订
- 增加 `draft/published` 发布状态机制
- 增加一键导出备份功能（JSON 下载）
- 确定编辑器方案 A：textarea + 实时预览分栏
- 风格调整为「学术风 + 杂志美感」：无衬线主字、宽行距、大留白
- 增加文章 emoji 标识功能
- 数据结构增加 `emoji` 和 `status` 字段

### 第 3～4 步完成：选择技术栈 & 初始化项目
- 确认技术栈：Vite + React 19 + TypeScript + Tailwind CSS 4 + react-markdown + react-router-dom + remark-gfm
- 使用 `pnpm create vite` 初始化 React + TypeScript 项目
- 安装所有依赖：react-router-dom、react-markdown、remark-gfm、tailwindcss、@tailwindcss/vite
- 配置 `vite.config.ts` 启用 Tailwind CSS 插件
- 清理 Vite 模板代码，替换为项目入口页
- 编写 `src/index.css` 全局样式（学术风 + 杂志美感 + Markdown 渲染样式）
- `pnpm build` 通过 ✅
- `pnpm dev` 启动成功 → http://localhost:5173/ ✅
- 更新 `AGENTS.md` 填写完整技术栈和构建命令

### 第 6 步完成：分阶段开发计划
- 输出 6 阶段开发计划，用户确认通过
- 阶段 1：基础设施（类型 + 存储 + 工具）
- 阶段 2：首页文章列表
- 阶段 3：文章详情阅读
- 阶段 4：写作页（新建文章）
- 阶段 5：编辑 & 删除
- 阶段 6：导出备份 + 收尾

### 阶段 1 完成：基础设施
- 创建 `src/types/article.ts` — Article 接口和类型定义
- 创建 `src/storage/articleStore.ts` — localStorage CRUD 完整实现
- 创建 `src/utils/helpers.ts` — 工具函数（ID生成、日期格式化、摘要提取、emoji预设）
- 种子数据：3 篇文章（2 篇 published + 1 篇 draft）
- `pnpm build` 通过 ✅

### 阶段 2 完成：首页文章列表
- 创建 `src/components/ArticleCard.tsx` — 文章卡片（emoji + 标题 + 摘要 + 日期）
- 创建 `src/pages/HomePage.tsx` — 首页（仅展示 published 文章，时间倒序，含写文章入口）
- 更新 `src/App.tsx` — 引入真正的 HomePage 组件
- 页面显示 2 篇种子数据 published 文章，草稿不显示
- `pnpm build` 通过 ✅

### 重命名：博謇的个人博客 → 玄牙的个人空间
- `src/pages/HomePage.tsx` 标题改为「玄牙的个人空间」
- `src/storage/articleStore.ts` localStorage key 改为 `xuanya-blog-articles`
- `README.md` 开发者名改为「祝欣缘（玄牙）」
- `docs/PRD.md` 项目名和用户引用全部更新

### 种子数据替换：ima 知识库 → 博客内容
- 从「博謇的知识库」中选取 3 篇原创内容作为种子文章
- 《降低启动成本：关于拖延、习惯与抑郁的一点思考》— 来源音频转录，个人反思
- 《天赋、觉醒与多支点生活》— 来源音频转录，哲学思辨
- 《研究摘要：dTMS 靶向 ACC 治疗精神分裂症》— 来源 TXT 文件，学术记录
- `pnpm build` 通过 ✅

### 阶段 3 完成：文章详情页
- 创建 `src/pages/ArticlePage.tsx` — 文章详情页（emoji + 标题 + 日期 + Markdown 正文）
- 使用 react-markdown + remark-gfm 渲染 GFM 格式
- 学术风阅读排版：prose-container 样式（18px 正文、1.8 行距、700px 居中）
- 路由 `/article/:id` 配置完成
- 文章不存在时自动跳转首页
- `pnpm build` 通过 ✅

### 阶段 4 完成：写作页 + Markdown 编辑器
- 创建 `src/components/MarkdownEditor.tsx` — 左右分栏编辑器（textarea + react-markdown 实时预览）
- 创建 `src/pages/WritePage.tsx` — 完整写作页（标题输入 + emoji 选择器 + 编辑器 + 发布/存草稿）
- emoji 选择器：16 个预设 emoji 网格，选中高亮
- "存为草稿" → 保存 status: 'draft' 后跳转首页
- "发布" → 保存 status: 'published' 后跳转首页
- 路由 `/write` 配置完成
- 编辑器样式：等宽字体 textarea（15px）、灰底标签栏、预览区复用 prose-container 样式
- `pnpm build` 通过 ✅

### 补充：首页增加草稿箱切换
- 首页右侧增加「已发布」/「草稿箱」切换按钮（Toggle 组件）
- 草稿箱按钮显示草稿数量角标
- 切换后列表实时过滤对应状态的文章
- 底部统计同时显示已发布数和草稿数
- `pnpm build` 通过 ✅

### 阶段 5 完成：编辑 & 删除
- 创建 `src/components/ConfirmDialog.tsx` — 确认弹窗组件（原生 dialog + 遮罩）
- `WritePage` 支持编辑模式：URL `/write/:id` 预填已有文章数据
- 编辑模式下按钮：删除 / 存为草稿 / 保存修改
- "保存修改" → 调用 updateArticle + 跳转回详情页
- "删除" → 弹出确认弹窗，确认后调用 deleteArticle + 跳转首页
- `ArticlePage` 底部增加「删除」按钮 + 草稿状态标识
- 路由 `/write/:id` 配置完成
- `pnpm build` 通过 ✅

### 阶段 6 完成：导出备份 + 收尾
- 创建 `src/utils/export.ts` — JSON 导出工具（一键下载全部文章备份）
- 首页增加「↓ 导出备份」按钮，文件命名格式 `xuanya-blog-backup-YYYY-MM-DD.json`
- UI 一致性修正：HomePage 容器统一 `max-w-3xl`、所有按钮统一 `rounded-lg`、返回箭头统一 `←`
- 最终 `pnpm build` 通过 ✅ — MVP 开发完成

### 补充：增加导入备份功能
- `src/storage/articleStore.ts` 新增 `importArticles()` 函数（合并模式，跳过已有 ID）
- `src/utils/export.ts` 新增 `parseBackupFile()`（文件选择器 + JSON 解析 + 格式验证）
- 首页增加「↑ 导入备份」按钮
- 导入流程：选择 JSON → 解析验证 → 确认弹窗（显示新/已有文章数）→ 合并导入 → 刷新列表
- `pnpm build` 通过 ✅

### 第 9 步完成：Git 保存版本
- `git init` 初始化仓库于 `D:\ai-blog\`
- 配置用户：玄牙 / 7154122001@email.edu.cn（仓库级）
- 首次 commit：32 文件，4623 行
